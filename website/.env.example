# Canonical site URL (sitemap + canonical links). No trailing slash.
# On Vercel, set this in Project Settings → Environment Variables if it differs.
# During domain cutover, temporarily pin to https://react-calendar-time.vercel.app
SITE_URL=
