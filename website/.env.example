# Canonical site URL (sitemap + canonical links). No trailing slash.
# On Vercel: Project Settings → Environment Variables → SITE_URL
# Must be the live host (Search Console fails if this is a redirected legacy URL).
SITE_URL=https://universal-datetime-picker.vercel.app
